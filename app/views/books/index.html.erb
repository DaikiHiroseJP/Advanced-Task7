<div class='container-fluid'>
  <%= render 'layouts/errors', obj: @book %>
  <div class='row'>
    <div class='form ml-5'>
      <h2>User info</h2>
      <%= render 'users/info', user: current_user %>
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @book %>
    </div>
    <div class='col-md-8'>
      <h2>Books</h2>
      <p>
       <%= link_to '新しい順', books_path(latest: "true") %>
       |<%= link_to '古い順', books_path(old: "true") %>
       |<%= link_to '評価の高い順', books_path(star_count: "true") %>
       |<%= link_to '一週間のいいねの多い順', books_path(favorite_week: "true") %>
      </p>
      <%= render 'index', books: @books, book_detail: @book_detail%>
    </div>

  </div>
</div>